# key modifier
# xmodmap ~/.xmodmaprc

# AMD/NVIDIA dual GPU
# xrandr --setprovideroutputsource modesetting NVIDIA-0
xrandr --output HDMI-0 --auto --output eDP-1-0 --auto --left-of HDMI-0
xrandr --dpi 96

# get a random wallpaper

